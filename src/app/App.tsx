import { AppRouter } from "@/app/routes";
import { BrowserRouter } from "react-router-dom";

export const App = () => {
    return (
        // <BrowserRouter>
        //     <AppRouter />
        // </BrowserRouter>
        <AppRouter />
    
    )
}